#include <string.h>
#include <vector.h>

class AST {
public:
    AST(std::string type, std::string type, int line, int column);

private:
    std::string type;
    std::string type;
    int line;
    int column;
};
